/**
 * Re-export of the Host UI Kit `TagFilterBar` (M34/L12). The component now lives
 * in the kit so host pages and runtime plugins render the exact same bar; this
 * shim keeps existing app imports (`../_shared/TagFilterBar`) working.
 */
export * from '../../host-ui-kit/list/TagFilterBar.js';
